/*
const main = (verb, noun) => {
    return verb + ' ' + noun
}
let cat = main (`calm`, `cat`)
console.log(main('happy' , 'dog'))
console.log(cat)
*/
/*
let calculateTriArea = (base, height) => 0.5 * base * height
//triangle values below
let base = 12
let height = 10
let area = calculateTriArea(base, height)

console.log(`The area of the triangle is: ${area}`)
*/

/*
let accnumber = 50449921

const cashWithdrawal = (amount, accnum) => {
    console.log(`Withdrawing ${amount} from account ${accnum}`)
}

cashWithdrawal(300,)
cashWithdrawal
cashWithdrawal
*/

let celsiusToFahrenheit = celsius => (celsius * 9/5) + 32;

const celsiusTemperature = 15;
let fahrenheitTemperature = celsiusToFahrenheit(celsiusTemperature);

console.log(`${celsiusTemperature}°C is ${fahrenheitTemperature}°F.`);